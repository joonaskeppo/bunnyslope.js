{
    "scripts": {
        "build:dev": "esbuild src/dom.js --global-name=b --format=iife --bundle --outdir=dev --allow-overwrite --watch --serve --servedir=dev",
        "test": "jest"
    },
    "devDependencies": {
        "esbuild": "0.20.1",
        "jest": "^29.7.0"
    }
}
